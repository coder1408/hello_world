#include<stdio.h>

void
main ()
{
  int a = 10, b = 20;
  int c;
  int d;
  int e;
  int f;
  c = a + b;
  d = a * b;
  e = b / a; 
  f = b - a;
  printf ("\tResult is=%d", c);
  printf ("\tResult is=%d", d);
  printf ("\tResult is=%d", e);
  printf ("\tResult is=%d", f);
}